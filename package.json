{
  "name": "it-tms",
  "private": true,
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "format": "turbo run format",
    "typecheck": "turbo run typecheck",
    "install:check": "pnpm install --frozen-lockfile",
    "install:update": "pnpm install && echo 'Lockfile updated successfully'"
  },
  "devDependencies": {
    "picocolors": "^1.1.1",
    "semver": "^7.7.2",
    "turbo": "2.0.10"
  },
  "dependencies": {
    "caniuse-lite": "^1.0.30001743",
    "sass": "^1.93.1"
  }
}